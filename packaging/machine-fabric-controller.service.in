[Unit]
Description=Machine Fabric Controller
After=default.target

[Service]
Type=simple
ExecStart=@BINARY@ --socket @SOCKET@ controller serve --state @STATE@ --id @CONTROLLER_ID@
Restart=on-failure
RestartSec=1

[Install]
WantedBy=default.target
