[Unit]
Description=Machine Fabric Executor
After=default.target

[Service]
Type=simple
ExecStart=@BINARY@ --socket @SOCKET@ executor serve --id @EXECUTOR_ID@@ALLOW_ROOTS@
Restart=on-failure
RestartSec=1

[Install]
WantedBy=default.target
