[Unit]
Description=Machine Fabric private clipboard display
Before=@EXECUTOR_SERVICE@

[Service]
Type=simple
ExecStart=@XVFB@ @DISPLAY@ -screen 0 16x16x24 -nolisten tcp -noreset -auth @XAUTHORITY@
Restart=on-failure
RestartSec=1

[Install]
WantedBy=default.target
